import preset from "@rebass/preset";

export const theme = {
  ...preset,
  colors: {
    ...preset.colors,
    overlay: "rgba(0, 0, 0, 0.15)",
    brand: "#62a861",
    primary: "#475abd"
  }
};
